spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://user-db:5432/userdb}
    username: ${POSTGRES_USER:user}
    password: ${POSTGRES_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: validate